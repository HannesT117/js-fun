# JS Fun

After rediscovering [WTFJS](https://github.com/denysdovhan/wtfjs), I took it as inspiration to implement some of the quirks of JS in JS.

## Run

It doesn't make sense to run anything else than the tests:

```sh
pnpm i
pnpm test
```
